import os
import sys
import csv
import requests

if __name__ == '__main__':
    if len(sys.argv) > 1:
        username = sys.argv[1]
        # Seu código entra aqui
    else:
        print("passe um username")
